# TODO

- [ ] Replace hero model: use `models/earth.glb` instead of `models/silent_ash.glb` in `src/components/3D/SilentAshModel.jsx`.
- [ ] Tune Earth model transform (scale/position) + rotation/camera so it sits correctly inside the hero right panel.
- [ ] Adjust `src/components/sections/Hero.module.css` to align the model vertically with the hero text.
- [ ] Verify responsive behavior (1024px and 768px breakpoints) visually/build.

